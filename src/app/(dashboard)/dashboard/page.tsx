// import { auth } from "@/auth";

const DashBoardPage = () => {
  // const session = await auth();
  // if (!session?.user) return null;

  return <div>This is a home page. - dashboard</div>;
};

export default DashBoardPage;
